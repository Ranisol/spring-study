# database setting, 테스트에서는 설정 없으면 인메모리 디비(jdbc:h2:mem)로 돌려줌
#spring:
#  datasource:
#    url: jdbc:h2:tcp://localhost/~/jpa-study
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create
#    generate-ddl: false
#    properties:
#      hibername:
#        format_sql:true

logging:
  level:
    org.hibernate.SQL : debug
    org.hibernate.type : trace